<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ask Mia - DriveNest AI</title>
  <style>
    body {
      background: radial-gradient(#0f0c29, #302b63, #24243e);
      color: #00ffe7;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      overflow: hidden;
    }
    .mia-avatar {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      background: url('https://i.imgur.com/AMiJeGG.png') no-repeat center center/cover;
      box-shadow: 0 0 25px #00ffe7;
      margin-bottom: 20px;
    }
    input, button {
      padding: 10px;
      border-radius: 5px;
      border: none;
      font-size: 16px;
    }
    input {
      width: 300px;
      margin-right: 10px;
    }
    button {
      background-color: #00ffe7;
      color: #0f0c29;
      cursor: pointer;
    }
    pre {
      margin-top: 20px;
      width: 80%;
      max-height: 200px;
      overflow-y: auto;
      background-color: rgba(0, 0, 0, 0.3);
      padding: 15px;
      border-radius: 8px;
      box-shadow: inset 0 0 10px #00ffe7;
    }
  </style>
</head>
<body>
  <div class="mia-avatar"></div>
  <div>
    <input type="text" id="userInput" placeholder="Ask Mia anything..." />
    <button onclick="askMia()">Ask</button>
  </div>
  <pre id="response">Mia is ready to help you 🚀</pre>

  <script>
    async function askMia() {
      const input = document.getElementById("userInput").value;
      const responseBox = document.getElementById("response");
      responseBox.innerText = "Mia is thinking... 🤔";

      const response = await fetch("https://YOUR_BACKEND_URL_HERE.com/mia", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ prompt: input })
      });

      const data = await response.json();
      responseBox.innerText = data.reply || "Mia didn't understand that.";
    }
  </script>
</body>
</html>

